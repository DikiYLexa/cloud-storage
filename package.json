{
  "name": "cloud-storage-monorepo",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build && cd ../backend && mkdir -p public && cp -r ../frontend/build/* public/",
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}